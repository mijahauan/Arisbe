# Cluster A findings (DEEP — all 7 dimensions)
Docs audited: docs/LINEAR_GRAPHICAL_CORRESPONDENCE.md, docs/EXACT_CORRESPONDENCE.md, docs/SOUNDNESS_BOUNDARY.md, docs/PERFORMANCE_ENVELOPE.md, docs/DAG_HISTORY_ARCHITECTURE.md, docs/UNIVERSE_OF_DISCOURSE_ARCHITECTURE.md, docs/CHAIN_OF_SEMIOSIS.md, docs/MANIFEST_AND_MEANING.md, docs/MEANING_BY_HISTORY.md, docs/LEVEL_ZERO_AND_THE_REGISTERS.md. All ten confirmed as book chapters in docs/_quarto.yml. Edited nothing.

- docs/LINEAR_GRAPHICAL_CORRESPONDENCE.md:299 · staleness · I · judgment · §8.1 L6 says "clockwise reading is implemented, clockwise placement enforcement in the layout is pending"; verified against src/clockwise_placement.py's `place_clockwise_hooks`, wired into `layout_service.py` (called pre-attestation for every style), and EXACT_CORRESPONDENCE.md §3c which documents this shipped **Done (2026-06-10)** — three days after this doc's "last revised 2026-06-07" · FIX: update L6/R8 to reflect `place_clockwise_hooks` as shipped (best-effort placement + single start-anchor numeral, reverting to full numbering only on a sound-revert), citing EXACT_CORRESPONDENCE.md §3c.
- docs/LINEAR_GRAPHICAL_CORRESPONDENCE.md:313-317 · staleness · I · judgment · §8.2 R8's closing parenthetical "(Pure geometric clockwise placement enforcement — no numerals at all — remains future layout work.)" reads as still-open, but EXACT_CORRESPONDENCE.md §3c documents the placement step now runs for every layout/style (the numeral is described there as reduced to a single optional start-anchor, not full numbering) · FIX: same as above — reconcile the two docs' "pending" vs "done" framing of clockwise placement in one wave.
- docs/EXACT_CORRESPONDENCE.md:101 · staleness · I · judgment · "Phase 1 — exact cut containment (the foundation). *In progress.*" — but Phases 2–4 (all dated Done 2026-06-10) structurally depend on Phase 1's `cut_boundary`/`point_in_cut` machinery, and `src/presentation_ops.py` already implements `cut_boundary` (line 416), `point_in_cut` (501), `bounds_in_cut` (525) exactly as Phase 1 specifies · FIX: mark Phase 1 Done (2026-06-10) to match its dependents, or state precisely what sub-piece of Phase 1 remains open if anything genuinely does.
- docs/UNIVERSE_OF_DISCOURSE_ARCHITECTURE.md:759-761 · staleness · I · judgment · Footer reads "**Last Updated**: 2025-10-14 … **Status**: Foundational Architecture Document … **Next Steps**: Model refactoring and implementation," directly contradicting the doc's own inline note at line 610, "**Status (2026-06-08):** this roadmap is largely realized" (UniverseOfDiscourse model, UoDCategory, TomosService, all three web modes, EPG engine — all shipped) · FIX: delete or rewrite the stale 2025-10-14 footer to agree with the 2026-06-08 status note already in the body.
- docs/UNIVERSE_OF_DISCOURSE_ARCHITECTURE.md:661-686 · staleness · I · judgment · The "Success Criteria" checklist is entirely unchecked (`- [ ]` throughout — conceptual clarity, data model, tomos organization, module functionality, UX), but the doc's own body confirms most of these are done (rename done at 322-324; all three modes live at 610-615) · FIX: check off completed items or replace the checklist with a status narrative pointing to docs/CAPABILITY_MAP.md.
- docs/UNIVERSE_OF_DISCOURSE_ARCHITECTURE.md:235-240 · named-entity consistency (EPG roles) · I · judgment · Describes **Graphist** as "(Defender/User): Asserts a graph, must defend it" and **Grapheus** as "(Challenger/System): Attempts to falsify by finding counterexample," with Goal(Grapheus)="reduce to empty sheet," Goal(Graphist)="defend assertion." This is the *inverse* of the canonical automated-EPG frame (CLAUDE.md's `agon_llm` bullet: Graphist·**doubt** stresses M, Grapheus·**defense** defends M) — here Graphist defends and Grapheus attacks. Component for the harmonizer: this doc supplies the *original two-player transformation-game* assignment (matches ENDOPOREUTIC_GAME_GUIDE.md's older table, per the pre-pass's EPG-mentions list), not the later interpretation-register assignment · FIX: no deletion — record as one more raw component in the cross-cutting EPG-role harmonization pass; note explicitly which "game" (two-player transformation contest vs. automated model-development) each assignment belongs to.
- docs/DAG_HISTORY_ARCHITECTURE.md:302-309 · staleness · I · judgment · "Future Enhancements" lists "Branch Labels: User-defined names for branches," "Branch Colors: Visual distinction in GUI," and "Diff Views: Compare states across branches" as not-yet-built, but `src/chain_branches.py`'s `branch_report`/`Branch`/`BranchReport` (wired into `web_api/routes/organon.py` at lines 583/730/886) already ships branch orientation (the ⑂ chip strip, per-branch counters) as of 2026-07-16 · FIX: move shipped items out of "Future Enhancements," cite `chain_branches.py`.
- docs/DAG_HISTORY_ARCHITECTURE.md (whole doc) · staleness · M · judgment · No status/date header at all, unlike every sibling architecture doc in this cluster (all of which carry a "Status"/"Created"/"Updated" line) and no mention of the newer, lighter `TransformationChain`/`ChainStep` model (`src/tomos_service.py`) that now coexists with the `EGITransformationHistory` DAG this doc documents (CLAUDE.md is explicit the two are NOT the same: "NOT a hydration of the protected EGITransformationHistory") · FIX: add a status line; add a short note distinguishing the two chain representations and when each is used.
- docs/DAG_HISTORY_ARCHITECTURE.md (whole doc) · readability · M · judgment · Book-tier chapter reads as a raw API dump (code blocks, bare identifiers like `HistoryBranchType.EXPLORATION` vs `.ALTERNATIVE`) with no expository prose distinguishing them, out of step with every other doc in this cluster's careful first-use definitions and GLOSSARY linking · FIX: add one or two sentences per `HistoryBranchType` value and link first-use jargon to GLOSSARY.md as the sibling docs do.
- docs/LEVEL_ZERO_AND_THE_REGISTERS.md:190-207 (§5) · staleness · I · judgment · States M "sits at odd depth, in a negative context" inside the scroll `cut[ M cut[P] ]` — the pre-"second relocation" picture. `docs/M_RESIDENCE_AND_THE_VALIDITY_DISCIPLINE.md` §9 (ratified 2026-07-16, after this doc's 2026-06-21 update, and which explicitly cites this doc's §5/§8 as a companion) relocated M's **standing residence** to cells at even depth beside the hold (`~[ ~[cell] … ~[ ] ]`). LEVEL_ZERO has no forward pointer to that ruling · FIX: add a note at §5 distinguishing the single-episode scroll antecedent (still odd depth, for one contest's local frame) from the corpus-standing residence of M (even-depth cells per M_RESIDENCE §9), with a forward link.
- docs/UNIVERSE_OF_DISCOURSE_ARCHITECTURE.md:738-755 · overclaim · M · judgment · Concluding rhetoric — "Arisbe transcends the limitations of diagram editors and becomes a true logical reasoning environment" — reads as an asserted terminus/marketing claim rather than the scoped, tiered honesty the newer docs in this cluster use (contrast SOUNDNESS_BOUNDARY.md's proven/verified/attested/argued tiers) · FIX: soften to match the hedged tone used elsewhere, or explicitly frame as the document's original (2025-10-14) aspirational conclusion, now superseded by CAPABILITY_MAP.md's factual status.

## Clean on the remaining ground
- **dead-refs**: all internal markdown links in these ten docs resolve to existing files (spot-checked programmatically); no additions to the pre-pass's zero-real-dead-links finding.
- **overclaim / "institutionalize"**: no misuse of "institutionalize" found in this cluster; no illegitimate "final opinion"-as-terminus assertions (MEANING_BY_HISTORY.md:61's "final forms" is about drawn-graph shape, not Peirce's final-opinion doctrine — not a finding).
- **vocabulary drift**: no "commens" usage in this cluster (not required here); no "mention-ascent" leftovers; "UoD" is used consistently with the canonical sense throughout, including in the two oldest docs (DAG_HISTORY_ARCHITECTURE.md, UNIVERSE_OF_DISCOURSE_ARCHITECTURE.md).
- **dedup**: no significant pure-redundancy found — apparent overlaps (e.g. DAG_HISTORY_ARCHITECTURE.md's "Use Cases" vs UNIVERSE_OF_DISCOURSE_ARCHITECTURE.md's "Complete Workflow"; the "chord"/correspondence material threading LINEAR_GRAPHICAL_CORRESPONDENCE.md, EXACT_CORRESPONDENCE.md, MANIFEST_AND_MEANING.md) are deliberate, cross-linked, complementary elaborations, not restatements.
- **readability (bare §N cross-doc refs)**: this cluster is otherwise disciplined — every cross-doc `§N` reference checked names its target document (e.g. MEANING_BY_HISTORY.md:58 "LEVEL_ZERO_AND_THE_REGISTERS.md §4," CHAIN_OF_SEMIOSIS.md:241 "MODALITY_WITHOUT_GAMMA.md §8"). The pervasive bare `§3.3` throughout (EXACT_CORRESPONDENCE.md, SOUNDNESS_BOUNDARY.md, CHAIN_OF_SEMIOSIS.md, MANIFEST_AND_MEANING.md, MEANING_BY_HISTORY.md, LEVEL_ZERO_AND_THE_REGISTERS.md, PERFORMANCE_ENVELOPE.md) is treated as an established project-wide anchor decoded by GLOSSARY.md's "Notation & reference numbers" section (and almost always appears wrapped in a gloss, e.g. "the correspondence check (§3.3)") — judged not a defect, not individually itemized.
- SOUNDNESS_BOUNDARY.md and PERFORMANCE_ENVELOPE.md: no staleness found — both read as actively maintained, cross-checked against CLAUDE.md's current module bullets (materialization/K3, live-runner throughput, ligature-router fixes all consistent).
