# Cluster G audit — layout/UI/workflow docs (LIGHT tier)

Auditor: cluster G. Files: docs/ADAPTIVE_SCOPE_VIEWER.md, docs/TENSION_LAYOUT.md,
docs/PRESENTATION_DELTAS_AND_STYLE.md, docs/STYLE_SYSTEM_GUIDE.md, docs/WEB_VIEWER_DESIGN.md,
docs/UI_TRANSPARENCY_CHARTER.md, docs/ELK_LAYOUT_IMPLEMENTATION_SUMMARY.md,
docs/TRANSFORMATION_WORKFLOW_SPEC.md, docs/COMPOSITION_WORKFLOW_SPEC.md,
docs/UNIVERSAL_GENERALIZATION_DAU_HOMEWORK.md, docs/ROADMAP.md.

Dimensions in scope (LIGHT tier): 2 (staleness), 4 (named-entity/EPG roles), 5 (vocabulary),
plus dead-refs only if the pre-pass missed one. Edited nothing; findings only.

## Findings

- docs/STYLE_SYSTEM_GUIDE.md:453-457 · staleness · I · judgment · "File Locations" lists
  `src/layout_engine_styled.py` and `src/style_integration.py` — neither file exists in `src/`
  (verified: only `style_loader.py` + `style_specification.py` are present; no
  `layout_engine_styled.py`/`style_integration.py` anywhere in the tree) · FIX: replace the file
  list with the real modules (`style_loader.py`, `style_specification.py`, `elk_layout_engine.py`)
  or drop the two dead entries.
- docs/STYLE_SYSTEM_GUIDE.md:421-431 · staleness · I · judgment · "Debug Tools" cites
  `tools/validate_style.py`, `tools/test_style_layout.py`, `tools/style_preview.py` — none exist
  under `tools/` (checked; absent) · FIX: remove or point at what actually exists
  (`tests/test_styles.py`, `tests/test_styled_layout_engine.py`, `tests/test_svg_style_rendering.py`).
- docs/ELK_LAYOUT_IMPLEMENTATION_SUMMARY.md:1-115 (whole doc, esp. "Core ELK Integration" §
  and "Known Limitations") · staleness · C · judgment · dated 2026-04-01 and describes
  `elk_layout_engine.py` as "395 lines" with a basic 3-tier ligature-routing strategy; the file is
  now 1300 lines (verified via `wc -l`) and carries substantial later work this doc never
  mentions: `rebuild_ligature_anchors` (perimeter-anchor re-derivation), the tension-order wiring
  (`considerModelOrder`/`sibling_order`), and (per CLAUDE.md's elk_layout_engine bullet, dated
  2026-07-02/03) an exact bbox quick-reject in `_seg_crosses_rect` plus a separation
  short-circuit + uniform-grid obstacle culling + lazy A* in `_route_via_visibility_graph`. A
  reader landing on this doc gets a materially incomplete/outdated picture of the routing
  algorithm and its performance characteristics · FIX: either retitle as an explicit historical
  snapshot ("as of 2026-04-01; superseded — see CLAUDE.md's elk_layout_engine bullet /
  TENSION_LAYOUT.md for later work") or refresh the "Core ELK Integration" and "Known
  Limitations" sections.
- docs/ELK_LAYOUT_IMPLEMENTATION_SUMMARY.md:182 · staleness · I · judgment · "Known
  Limitations" #1 says "No incremental layout: ... unchanged elements don't automatically hold
  their positions (deferred to future work)" — this is superseded by
  TRANSFORMATION_WORKFLOW_SPEC.md's ④a (pin-and-place / subtractive + additive layout, shipped
  2026-06-06: `layout_service._subtractive_layout` / `_additive_cut_layout`, verified 0px drift)
  and the tension engine's hierarchical constrained-stress placement · FIX: drop the limitation
  or forward-link to TRANSFORMATION_WORKFLOW_SPEC.md §3 (④a).
- docs/TENSION_LAYOUT.md:3 · staleness · I · judgment · header "Status: design speculation +
  proof-of-concept (2026-06-07). Not built into the layout path." directly contradicts the same
  document's own §8-§11 ("Wired into the layout pass (2026-06-07)", "Built — increment 1
  (2026-06-07)", "Wired into the engine (2026-06-07)", 18/18 corpus attesting) — the tension
  ordering *is* wired into `ELKLayoutEngine` (sibling order via `considerModelOrder`) and a full
  alternate `TensionLayoutEngine` was built the same day the doc was written, per its own later
  sections · FIX: update the header status line to reflect same-day build (e.g. "design
  speculation → PoC → wired same day, 2026-06-07; see §8-§12 for what shipped").
- docs/TRANSFORMATION_WORKFLOW_SPEC.md:3 · staleness · I · judgment · header "Status: reviewed
  design (2026-06-04), not yet implemented" contradicts §5 "Build order," whose four items are
  each marked done/complete and which closes "All four are now complete: items 1-3 were largely
  additive; item 4 was the only protected-module change" · FIX: update the header Status line
  (e.g. "design 2026-06-04, implemented 2026-06-04→06-06 — see §5 Build order").
- docs/UNIVERSAL_GENERALIZATION_DAU_HOMEWORK.md (whole doc, esp. §3 "Implementation notes (for
  the 'finally close ∀x' step)") · staleness · I · judgment · presents `universal_generalization`
  as a derived tactic still to be written ("belongs beside `existential_generalization`... add a
  test_totality_universal that runs the scaffold") with no completion marker, but it is already
  implemented: `universal_generalization` exists in `src/derived_rules.py:204` (exported in
  `__all__`) and `tests/test_induction_proofs.py:798` defines `test_totality_universal` exactly as
  described · FIX: add a "✅ BUILT" status line pointing at `derived_rules.universal_generalization`
  + `test_induction_proofs.py::test_totality_universal`, matching the done-marker convention used
  elsewhere in this cluster (e.g. ADAPTIVE_SCOPE_VIEWER.md's "✅ DONE" entries).

## Named-entity (dimension 4 — EPG roles)

This cluster is almost entirely outside Agon, so the Graphist/Grapheus/Agonothetes triad barely
appears. One hit:

- docs/WEB_VIEWER_DESIGN.md:54 · named-entity · M · judgment · the only EPG-role mention in this
  cluster, in the design-token table: "Agon roles (by polarity) | `--role-graphist` (blue) ·
  `--role-grapheus` (maroon)". Informative component contributed: **UI color-coding only** (no
  role/stance/mechanical-reduction description — consistent with, not contradicting, the
  canonical frame). Recorded for the harmonizer's raw-component pool; no fix needed on its own.

## Vocabulary drift (dimension 5)

Clean. UoD usage across the cluster (ADAPTIVE_SCOPE_VIEWER.md, PRESENTATION_DELTAS_AND_STYLE.md,
TENSION_LAYOUT.md, COMPOSITION_WORKFLOW_SPEC.md, ROADMAP.md) matches the settled meaning
(per-instance internalized/attested model); no doc in this cluster uses "commens" incorrectly.
ROADMAP.md:90 uses "mention-ascent" as a candidate paper title ("Conservative mention-ascent") —
checked against GLOSSARY.md:328 ("this term retires the earlier name 'the second-order
crossing'"): mention-ascent is the *current* preferred term, so this is correct usage, not drift.

## Dead-refs

None found beyond the pre-pass's triage; no new dead links spotted in this cluster.

## Clean

docs/PRESENTATION_DELTAS_AND_STYLE.md, docs/UI_TRANSPARENCY_CHARTER.md,
docs/COMPOSITION_WORKFLOW_SPEC.md, docs/ROADMAP.md — no staleness, named-entity, or vocabulary
issues found (ROADMAP.md was rewritten 2026-07-20 per the dispatch note and reads clean).

**Findings count: 7** (6 staleness + 1 named-entity data-point; 0 dead-refs; vocabulary clean).
