# Cluster H — findings (tier: LIGHT)

Files: docs/DEFINITION_NODE.md, docs/REFERENCE_AND_TRANSCLUSION_NODE.md,
docs/SCHEMA_HOLE_CORRESPONDENCE.md, docs/CORE_API_USAGE_GUIDE.md,
docs/CORRESPONDENCE_CONTRACT.md, docs/MCP_VERIFIER.md, docs/PROOF_SERIALIZER.md,
docs/CORPUS_AND_IMPORT_MODEL.md, docs/RETURN_TO_DEVELOPMENT.md, docs/ARCHIVE_INDEX.md,
docs/ARISBE_EXISTENTIAL_GRAPH_DEFINITION.md, docs/ALPHA_BETA_UX_DOCKET.md,
docs/JARGON_AUDIT.md, docs/STORM_DOCS_AUDIT.md

Dimensions in scope per brief: 2 (staleness), 4 (named-entity/EPG roles), 5 (vocabulary),
dead-refs only if pre-pass missed one.

## Dimension 4 (named-entity, EPG roles) — CLEAN
No Graphist/Grapheus/Agonothetes role descriptions in this cluster (grep-verified across
all 14 files; JARGON_AUDIT.md's 3 hits are its own meta-discussion of the *word*
"Agonothetes" as a jargon-audit entry, not a role description — not a dimension-4 item).

## Dimension 5 (vocabulary: UoD / commens / mention-ascent) — CLEAN
No hits for "commens" or "mention-ascent" anywhere in the cluster; no UoD-vocabulary drift
observed in the files read.

## Dimension 2 (staleness) — findings

- docs/CORE_API_USAGE_GUIDE.md:11 · staleness · C · judgment · "16 protected core modules" — `tools/core_protection_system.py`'s `protected_modules` set today has 14 entries (EGIF/CGIF/CLIF parsers/generators removed 2026-06-27; per CLAUDE.md "14 modules in src/ are protected") · FIX: update the count to 14 and regenerate this guide's module index from `tools/core_protection_system.py` / `ARISBE_CORE_API_REFERENCE.md`.
- docs/CORE_API_USAGE_GUIDE.md:58-59 · staleness · C · judgment · Pattern 1's `create_edge(relation="Human")` no longer matches `src/egi_core_dau.py`'s actual signature `def create_edge() -> Edge` (zero parameters today) — running this example raises `TypeError` · FIX: rewrite the pattern to build an `Edge` and attach `rel`/`nu` via the current constructor path (e.g. through `RelationalGraphWithCuts.with_edge` + the `rel`/`nu` maps).
- docs/CORE_API_USAGE_GUIDE.md:130-131 · staleness · C · judgment · `create_cut(area_id="cut_1")` no longer matches `def create_cut() -> Cut` (zero parameters today) — same crash-on-copy-paste problem as above · FIX: update the example to the current no-arg signature.
- docs/CORE_API_USAGE_GUIDE.md:140-142 · staleness · I · judgment · `index.get_nesting_info("cut_1")` — `HierarchicalIndex` has no `get_nesting_info` method (verified methods: `get_polarity`, `get_nesting_level`, `get_parent`, `get_children`, `get_ancestors`, etc.); there is no `NestingInfo`-returning accessor at all · FIX: replace with `index.get_polarity("cut_1")` / `index.get_nesting_level("cut_1")`.
- docs/CORE_API_USAGE_GUIDE.md:20-40 · staleness · I · judgment · lists `cgif_parser_dau`/`egif_parser_dau` under "core operations/parsing" as if still protected-core, and lists `enhanced_ligature_algorithms`, `ligature_optimization_engine`, `ligature_aware_positioning_engine`, `obstacle_aware_ligature_router`, `area_spatial_constraint_system` as protected modules — none of these five exist in `src/` (confirmed absent on disk; `tools/core_protection_system.py`'s own comments call four of them "ghosts" removed May 2026, and the linear-format parsers were explicitly de-protected 2026-06-27 as application-level I/O) · FIX: regenerate the module index from the current protected set; the EGIF/CGIF/CLIF parsers can stay documented but relabeled as non-core application I/O.
- docs/CORE_API_USAGE_GUIDE.md:3 · staleness · M · judgment · dated 2025-01-19 with no later "verified" marker, while the guide instructs readers to treat it as the single source of truth for signatures that have since changed (see findings above) · FIX: add a last-verified date or regenerate alongside `tools/extract_core_api.py`'s output.
- docs/RETURN_TO_DEVELOPMENT.md:45 · staleness · I · judgment · "Protected modules (17, listed in `tools/core_protection_system.py`)" — the live set has 14 entries today (same count CLAUDE.md states) · FIX: update to 14.
- docs/RETURN_TO_DEVELOPMENT.md:50-51 · staleness · I · judgment · "src/ — core modules (~32 .py files...)" and "tests/ — pytest suite (~955 passing, 35 skipped)" — current `src/*.py` top-level count is 118, and CLAUDE.md's current figure is "~1000 passing, 35 skipped" · FIX: refresh the counts or replace with a live-count instruction (the doc already tells the reader to run `pytest`; the stated numbers should not contradict that).
- docs/RETURN_TO_DEVELOPMENT.md:84-87 · staleness · C · judgment · "Three-mode UI... Agon shipped as a thin V1 arena 2026-06-01. The next frontier is deepening Agon" — this badly undersells current Agon capability (interpretation register, model materialization, the automated/LLM-driven Endoporeutic Game, meta-learning, live runners — all per CLAUDE.md, built through 2026-07) — a returning reader following only this doc's 5-minute checklist would think Agon is still a bare V1 arena · FIX: drop the stale snapshot and point to `docs/CAPABILITY_MAP.md`/`docs/ROADMAP.md` for current Agon status instead of restating a dated claim.
- docs/RETURN_TO_DEVELOPMENT.md:80-81 · staleness · M · judgment · "Rule-reversibility and closure-idempotence property tests are still to be written (carried forward from issue #4 into issue #8)" — unverified whether these are still open; flag for verification rather than assume · FIX: confirm issue #4/#8 status or remove the stale reference if resolved.
- docs/ARCHIVE_INDEX.md:289-305 · staleness · I · judgment · "What Remains Active" section states src/ 177 files, tests/ 58 files, tools/ 88 files, docs/ 62 files, styles/ 8 files — current counts are src/ 118 top-level .py, tests/ 172, tools/ 131, docs/ 87, styles/ 9 (tests/tools/docs have roughly tripled/doubled since; src/ shrank due to the Qt-GUI archive) · FIX: either drop the specific counts (this is a point-in-time cleanup record, not a living inventory) or caption them explicitly "as of 2026-06-08, not current."
- docs/ARISBE_EXISTENTIAL_GRAPH_DEFINITION.md (whole doc, no line-addressable claim beyond the file itself) · staleness · I · judgment · undated early-vision doc (git history shows only 2025-09-15 and 2026-03-21 touches) describing a 4-phase "Implementation Strategy" and an `ArisbeExistentialGraph` dataclass that was never built as designed, and characterizing Ergasterion as "Transitional between exemplar and comprehensive... for learners/experimenters" — at odds with CLAUDE.md's current Ergasterion role ("workshop"/private editor for drafts, regime-1 composition). It's referenced only from `docs/ALPHA_RELEASE_PLAN.md` and the doc-sweep plan itself, not from VISION_AND_SCOPE.md or CAPABILITY_MAP.md which superseded it · FIX: either move to `docs/archived/` with a banner pointing at VISION_AND_SCOPE.md + CAPABILITY_MAP.md (the pattern used for other superseded docs per ARCHIVE_INDEX.md's per-doc retirement list), or add a status banner if any part is still meant as a live statement.
- docs/JARGON_AUDIT.md:1 · staleness · I · judgment · header states "Status: proposal awaiting author sign-off (2026-06-30)" but the body's own "Resolved decisions (author, 2026-06-30)" section (lines 89-100) shows sign-off already happened the same day, and `docs/GLOSSARY.md` confirms every ⊕-flagged term (Agonothetes, recto, verso, tincture, teridentity, floor, membrane, style ladder) already has an entry — so the "After sign-off" checklist (lines 102-110) describes already-completed work as pending · FIX: update the header to "APPLIED (2026-06-30)" and strike or check off the after-sign-off checklist items now that the Glossary entries exist.

## Informational (not a finding — for the merge pass)

- docs/STORM_DOCS_AUDIT.md — this prior-audit doc is internally consistent and self-closing: every G-item is marked DISPOSED/ANSWERED and the doc states outright "Nothing from the STORM docket remains open" (line 129, dated 2026-07-07). No stale claim found; flagging only so the merge pass doesn't mistake it for an open docket.
- docs/CORRESPONDENCE_CONTRACT.md, docs/MCP_VERIFIER.md, docs/PROOF_SERIALIZER.md, docs/CORPUS_AND_IMPORT_MODEL.md, docs/DEFINITION_NODE.md, docs/REFERENCE_AND_TRANSCLUSION_NODE.md, docs/SCHEMA_HOLE_CORRESPONDENCE.md, docs/ALPHA_BETA_UX_DOCKET.md — read in full; no staleness, EPG-role, or vocabulary-drift findings. CORPUS_AND_IMPORT_MODEL.md's item counts (20 → 23 items) and ALPHA_BETA_UX_DOCKET.md's "whole docket shipped" status read as internally consistent with each other and with CLAUDE.md's current module descriptions; not re-verified against a live corpus listing (out of scope for LIGHT tier / would require running the app).
