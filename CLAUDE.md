# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> **Start here each session:** active work and the next-session handoff live in
> [CURRENT_PLAN.md](CURRENT_PLAN.md) (`▶ NEXT SESSION` section) — read it first.
>
> **Orienting (the structural spine, consolidated 2026-06-27):**
> [docs/VISION_AND_SCOPE.md](docs/VISION_AND_SCOPE.md) (what/why/scope/bedrock) ·
> [docs/CAPABILITY_MAP.md](docs/CAPABILITY_MAP.md) (what works, where, what guards it) ·
> [docs/ROADMAP.md](docs/ROADMAP.md) (what's next) · [docs/GLOSSARY.md](docs/GLOSSARY.md)
> (terms + reading order). Read these for the top-down view; CURRENT_PLAN is the chronological log.

## What This Project Is

Arisbe is an environment for **doing logic in pictures, not pictures of logic** — Charles Sanders Peirce's "moving pictures of thought" made operational. Frithjof Dau's formalization is the guarantor of correctness; that bedrock is non-negotiable. The **central engineering and research problem** the codebase exists to solve is the **inerrant correspondence between an EGI's linear written form and its graphical drawn form** — picture and proposition denoting the same mathematical object across every transformation, every layout regeneration, every user edit, every round-trip.

The fundamental entity is the **Universe of Discourse (UoD)** — a diachronic (evolving) process of logical reasoning, not a static diagram. A single EG is a synchronic snapshot within that process. The correspondence invariant is scoped to three regimes: **composition** (Ergasterion drafts, invariant suspended); **asserted/canonical** (Agon, Organon, every rule application, invariant mandatory and runtime-attested); **presentation-only** (always free, preserved by construction via the `presentation_ops` API). See [docs/LINEAR_GRAPHICAL_CORRESPONDENCE.md](docs/LINEAR_GRAPHICAL_CORRESPONDENCE.md) for the full contract.

## Generic Workflow Orchestration

### 1. Plan Mode Default
- Enter plan mode for ANY non-trivial task (3+ steps or architectural decisions)
- If something goes sideways, STOP and re-plan immediately — don't keep pushing
- Use plan mode for verification steps, not just building
- Write detailed specs upfront to reduce ambiguity

### 2. Subagent Strategy
- Use subagents liberally to keep main context window clean
- Offload research, exploration, and parallel analysis to subagents
- For complex problems, throw more compute at it via subagents
- One task per subagent for focused execution

### 3. Self-Improvement Loop
- After ANY correction from the user: update `tasks/lessons.md` with the pattern
- Write rules for yourself that prevent the same mistake
- Ruthlessly iterate on these lessons until mistake rate drops
- Review lessons at session start for relevant project

### 4. Verification Before Done
- Never mark a task complete without proving it works
- Diff behavior between main and your changes when relevant
- Ask yourself: "Would a staff engineer approve this?"
- Run tests, check logs, demonstrate correctness

### 5. Demand Elegance (Balanced)
- For non-trivial changes: pause and ask "is there a more elegant way?"
- If a fix feels hacky: "Knowing everything I know now, implement the elegant solution"
- Skip this for simple, obvious fixes — don't over-engineer
- Challenge your own work before presenting it

### 6. Autonomous Bug Fixing
- When given a bug report: just fix it. Don't ask for hand-holding
- Point at logs, errors, failing tests — then resolve them
- Zero context switching required from the user
- Go fix failing CI tests without being told how

## Task Management

1. **Plan First**: Write plan to `tasks/todo.md` with checkable items
2. **Verify Plan**: Check in before starting implementation
3. **Track Progress**: Mark items complete as you go
4. **Explain Changes**: High-level summary at each step
5. **Document Results**: Add review section to `tasks/todo.md`
6. **Capture Lessons**: Update `tasks/lessons.md` after corrections

## Core Principles

- **Simplicity First**: Make every change as simple as possible. Impact minimal code.
- **No Laziness**: Find root causes. No temporary fixes. Senior developer standards.
- **Minimal Impact**: Changes should only touch what's necessary. Avoid introducing bugs.

1. Don’t assume. Don’t hide confusion. Surface tradeoffs.

2. Minimum code that solves the problem. Nothing speculative.

3. Touch only what you must. Clean up only your own mess.

4. Define success criteria. Loop until verified.

## Environment & Commands

Dependencies are managed by **uv** (Python 3.12). One-time setup: `uv sync --extra dev --extra web` (the `web` extra carries FastAPI/uvicorn — the route tests and the web viewer need it; `uv sync` is exact and will prune them if you omit it). Run commands via `uv run` (no manual activation needed), or `source .venv/bin/activate` first.

```bash
# Testing
uv run pytest tests/ -q          # Full test suite (quiet)
uv run pytest tests/test_foo.py  # Single test file

# Quality assurance
uv run python tools/quality_gate_system.py         # Pre-commit checks (auto-run on commit)
uv run python tools/core_protection_system.py --report  # Check protected module status
uv run python tools/daily_quality_dashboard.py     # Overall system status

# Web viewer (canonical UI as of May 2026)
uv run uvicorn --app-dir src web_api.main:app --reload --port 8000   # API + static viewer at /
```

The Qt-based GUI (`arisbe.py`, `src/gui_clean/`) and its `unified_d3` layout
engine were archived to `archive/qt-gui-2025/` in May 2026 — see that
directory's README for context. They remain in git history if needed.

## Core Protection System

**14 modules in `src/` are protected** and cannot be modified without authorization — the genuine
calculus core: the data model + IO, diachronic state, the Dau rules + validators, the ligature
machinery, and the correspondence enforcers `correspondence_attestation` / `presentation_ops` /
`natural_layout` (the three added 2026-06-27). The EGIF/CGIF/CLIF parsers/generators were *removed* from
the set on 2026-06-27 as application-level I/O guarded by corpus round-trip tests, not the calculus:

```bash
touch .core_modification_authorized   # Required before modifying protected modules
python tools/core_protection_system.py --report  # Check what's protected
```

**The mathematical core test suite must always pass.** The core suite is the subset of `tests/` that covers `egi_core_dau`, `formal_transformation_rules`, `rule_interaction`, `subgraph_closure_validator`, `graph_isomorphism_engine`, and the Beta/logical proof exercises (~118 tests today). Failing core tests indicate real mathematical correctness issues, not test infrastructure problems.

## Architecture

### Three-Mode Conceptual Architecture

| Mode | Greek meaning | Role |
|------|--------------|------|
| **Organon** | "instrument" | Archive/corpus browser, timeline navigation, read-only |
| **Ergasterion** | "workshop" | Private editor, transformation practice, draft graphs |
| **Agon** | "contest" | Endoporeutic Game engine, formal validation, official record |

These are *conceptual modes*. The original Qt implementation (`src/gui_clean/`)
that mirrored them as separate windows was archived in May 2026. The current
plan is to surface them as routes within the web app (`src/web_api/`,
`src/web_viewer/`); that mapping is still ahead.

### Key `src/` Modules

- `egi_core_dau.py` — `RelationalGraphWithCuts` data model (immutable)
- `formal_transformation_rules.py` — Six Dau rules: ERA, INS, IT+, IT−, DC+, DC− (Beta-aware)
- `rule_interaction.py` — Headless RuleInteraction protocol for stepwise proof construction
- `eg_navigation.py` — Content-addressable structural selection over an EGI: name elements by what they are / where they sit (`cut_holding_relation`, `empty_cut_in`, `child_edges`, `edges_on_vertex`, `vertex_by_label`), `area_signature` (Alpha) + `same_graph` (full iso, the Beta authority), `describe` (kind/area/polarity introspection). The query half of the proof-authoring layer; area topology delegates to `presentation_ops`.
- `proof_authoring.py` — `ProofChain`: fluent builder that applies Dau rules by *locator* (`callable(egi)→id`, resolved against the current state) and records each `ChainStep` with deterministic ids/timestamps; `apply_rule` / `replay_step` are the shared engine/replay primitives. The action half — turns the dogfood friction (authoring by ephemeral id) into a readable chain. Used by `tools/build_*_chain.py`.
- `subgraph_closure_validator.py` — Closure validation (Beta-aware: free outer-area vertices)
- `universe_of_discourse.py` — UoD entity (synchronic EGI + diachronic DAG history + layout deltas)
- `egi_transformation_history.py` — DAG-based branching transformation history
- `endoporeutic_game.py` — Two-player dialogical game engine
- `elk_layout_engine.py` (+ `elk_worker.js`) — Cut-aware ELK-based layout, the default layout path; also `rebuild_ligature_anchors` (re-derive ligature endpoints from element geometry after a regime-3 move). The ligature router's visibility graph carries an **exact bbox quick reject** in `_seg_crosses_rect` (2026-07-02: a ~50-atom star-shaped M — one hub individual, the Wikidata entity shape — attested in 452s without it, 3.2s with; strict inequalities, bit-identical routes)
- `tension_layout.py` — the vertex tree as organizing principle: `sibling_order` (tension-minimizing free order of an area's siblings, the `?tension` knob) + `stress_majorize` (SMACOF). Correspondence-safe: a crossing-sequence is order-independent. See `docs/TENSION_LAYOUT.md`
- `tension_engine.py` — `TensionLayoutEngine`, an opt-in alternative projection (`?engine=tension`): hierarchical constrained stress with crossing-point proxies that places a relation *between* its arguments (Peircean single-line reading). Containment by construction; §3.3-gated with ELK fallback (17/18 corpus attest)
- `simple_svg_renderer.py` — LayoutDTO → SVG
- `peirce_latex.py` (+ `tex/arisbe-eg.sty`) — **authentic-Peirce LaTeX/TikZ export** (`export_peirce_latex(dto, egi)`), the Peirce-Edition-Project output path (ROADMAP #14, phase 1). Reimplements the *function* of Jukka Nikulainen's `egpeirce.sty` (oval cuts, scrolls, heavy lines of identity, hooks, argument order) in **pure TikZ that compiles with plain pdflatex** — no PSTricks. Two improvements egpeirce never attempted: **wedded to the EGI** (a pure function of the §3.3-attested `LayoutDTO` — ovals at `cut_bounds`, heavy lines from `vertex_positions` to a hook on the `predicate→points[0]` ray that *preserves the hook angle*, so the picture *is* the DTO and `eg_reader.read_drawing`+`reading_matches_egi` vouch for it; every element id traced in a `%` comment) and **delta-faithful** ("export what you *adjusted* to see" — regime-3 nudges thread through). `arisbe-eg.sty` is a modern, hand-authorable semantic macro layer (`\egcut`/`\egloi`/`\egdot`/`\egpred`/`\egconst`/`\egnum`, key-value `\egset`) the exporter inlines for a self-contained standalone document (`article` fallback on minimal installs). Wired into `web_api/services/export_service.py` as the `peirce-tikz` format beside the geometric `tikz` path; `export_egi` gained `deltas`/`previous_layout` pass-through. **Phase 2 (shipped):** an **iconic self-continuing scroll glyph** (`scroll_glyph=` — `~[A ~[B]]` drawn as the outer cut with a self-crossing neck wrapping the inner oval; ink-only, so `cut_bounds`/§3.3/`read_drawing` are untouched, like the waver/bridges); a **worked-chain → multi-figure LaTeX document** (`export_peirce_chain` + `export_peirce_chain_document` + `POST /export/chain` — one captioned figure per proof step); and HTTP route fields `ExportRequest.deltas`/`scroll_glyph` + `ExportChainRequest`. **Scholarly-publishing pass (2026-06-30):** an optional `caption=` (a TeX-escaped citation line under the figure, ink *outside* the picture so §3.3 is untouched) + `export_service.export_peirce_document` (an **appendix of several UoDs**, one captioned figure each) + `POST /export/document`. Deferred: the drawing→EGI learning loop.
- `scholarly_citation.py` — turn a UoD's **provenance into a publication citation** (the Peirce-Edition publishing path, [docs/FEATURE_PEIRCE_SCHOLARLY_REPRODUCTION.md](docs/FEATURE_PEIRCE_SCHOLARLY_REPRODUCTION.md)): `format_citation` (a human author-date line from a CSL record), `bibtex_entry` (CSL `type`→BibTeX entry type), `citation_for` (the bundle `{citation, bibtex, key, has_source}` — prefers `theorem_source`, falls back to a transcribed proof / first method / free-text `source_citation`). **Fabricates nothing**: an absent field is omitted, a sourceless graph reports `has_source: false`. Self-contained (no web/layout/tomos imports). Surfaced at `GET /export/citation`, the `ExportRequest.cite` flag (→ the figure caption), and the **"cite" checkbox** in Organon's export panel.
- `layout_learning.py` — the **drawing→EGI learning loop** (ROADMAP #14 (d)): `arrangement_deltas(egi, canonical_dto, drawn_dto)` recovers the regime-3 `PresentationDelta`s carrying Arisbe's canonical layout to a human-drawn arrangement of the *same* EGI (the inverse of `presentation_deltas.apply_deltas` — move_vertex/predicate/cut + reshape_cut, tagged by `describe` for the ladder); `generalize_arrangement` is the loop-closing pass-through to `extrapolate_deltas` (crystallise the learned placements onto untouched siblings → a refined Peirce-style regularity). The Peirce-Edition replica-then-parse signal made into the codebase's delta currency; reads only regime-3 facts, so it's correspondence-safe.
- `layout_dto.py` — Platform-independent layout DTO shared by layout engines and renderers
- `presentation_ops.py` — Regime-3 algebra: `move_vertex`, `move_predicate`, `reshape_cut`, `move_cut` (rigid translate of a cut + contents), `reroute_ligature`, each raising `Regime3Violation` on attempted boundary crossings. Also exports the public area-topology helpers (`element_area`, `cut_parents`, `area_chain`, `crossing_sequence`, `deepest_containing_cut`). `area_chain` (allowed areas, incl. LCA) and `crossing_sequence` (ordered cuts crossed, excl. LCA) share one `_tree_path` walk — the single source of truth consumed by `natural_layout.authorized_crossings` and `ELKLayoutEngine._authorized_cuts`.
- `natural_layout.py` — Coordinate-free projection-independent layout: `NaturalLayout` (containment tree, per-ligature required crossing-sequence, incidence, ports) built by `natural_layout(egi)`. The "own the dimensionality" layer; imports no geometry so a future 3-D projection is additive. See `docs/LINEAR_GRAPHICAL_CORRESPONDENCE.md` §3.1–3.2.
- `correspondence_attestation.py` — Runtime §3.3 check: `attest_correspondence(egi, dto)` raises `CorrespondenceViolation` on failure. Hooked into `web_api/services/layout_service.py` so every served (EGI, drawing) pair is verified.
- `eg_reader.py` — the **drawn→EG** reader (inverse of layout/render): `read_drawing(dto)` recovers the area tree + ordered incidence from geometry alone (inside/outside the drawn cut curves; tracing ligatures); `reading_matches_egi`, `assign_order_labels` (carry argument order with the fewest drawn numerals). De-risked on human geometry — the foundation of freeform composition.
- `drawing_validity.py` — fix-time well-formedness of a freeform drawing, in EG vocabulary: `validate_drawing(dto) → ValidityReport` (errors `overlapping_cuts` / `dangling_line`; warnings `boundary_band` / `unwired_predicate` / `label_overlap`). The twin of `correspondence_attestation` for a drawing with *no EGI yet*. See `docs/FREEFORM_COMPOSITION_AND_LEARNING.md`.
- `drawing_to_egi.py` — `build_egi_from_drawing(dto, predicate_labels, vertex_labels)`: the construction half of *fix = read* — joins recovered structure (`read_drawing`) with carried content (relation names, constants) into a real EGI. Corpus round-trip via `same_graph`.
- `egi_diff.py` — `legible_diff(target, attempt) → DiffReport`: the discrepancy report (the *how-they-differ* to `same_graph`'s yes/no), in EG terms — `structure`/`missing`/`extra`/`scope`/`incidence`/`order`. Content-aligned, not id-aligned (constants by label, relations by name + arg signature, generic lines by incidence). Powers challenge mode.
- `challenge_mode.py` — Freeform step 4: a curated `CHALLENGE_BANK` difficulty gradient + `grade(target, attempt) → DiffReport` (`same_graph` + the legible diff). Correspondence learned by doing — draw a target freehand, get graded in EG vocabulary.
- **The Agon interpretation register** (`docs/GENERATION_AND_TESTING.md`, `docs/DOMAIN_ORACLE_AND_M.md`) — the episode *given M, then G*: choose M → peel → decide. The conceptual cut is **eliminative (the game = Agon/testing) vs additive (making = Ergasterion)**; deduction earns the corpus through Agon (self-certifies validity, not warrant).
  - `domain_oracle.py` — M is **queried, not held**: a thin `DomainOracle` (`resolve` / `witness` / `match_atoms` / `individuals`), backed by `CorpusOracle` over local EGIs. "Enough" of M = what the proposal touches (vocabulary-bounded + open-world horizon).
  - `semantic_game.py` — `evaluate(egi, oracle)` reads G **outside-in** (the peel), asking the oracle at each negation-free layer; returns three-valued Kleene `Verdict3` (TRUE/FALSE/UNKNOWN, sound open-world) + transcript + structured `winning_witness` / `counterexample`. Model-checking, **not** inference; truth-in-a-model, **not** validity.
  - `model_materialization.py` — `materialize_egi(egi) → (facts_egi, report)`: forward-chain M's **Horn fragment** (`~[ B ~[ H ] ]`, range-restricted) to the least Herbrand model so a model authored as *facts + rules* is testable (the syllogism works); non-Horn shapes left to the contest game with an honest skip-report. **A model is the facts; rules are a theory.**
  - `theory_query.py` — `entails(theory, query)`: is the universal G a **theorem of the theory M**? The deduction step *ontology-as-M* needs — model-checking a subsumption over a pure T-box (SUMO, no individuals) reads *vacuously* TRUE/UNKNOWN, so decide it by **freeze-a-fresh-witness** (assert G's body over arbitrary constants → materialize M over them → check the head). Sound (witnesses arbitrary) + Horn-complete; a negative is FALSE only when M is wholly Horn, else UNKNOWN (skipped non-Horn axioms might bear). Wired into `/agon/interpret` as a `theorem` block beside the extensional verdict. See `docs/DOMAIN_ORACLE_AND_M.md` §6.2.
  - `agon_models.py` — curated reference-model scenarios (the persona episodes) for the `/agon` model picker; corpus UoDs are the other source. The **inverse pivot** (`/agon/where-it-holds`) ranges the peel across these to answer "in what domain does G hold?" — holds / partial (residue = contribution) / independent / contradicts.
  - `m_render.py` — **render M** (read-only chrome, M drawn but never asserted): `vocabulary_overlap` = the ground/legend (how G's and M's vocabularies meet — shared / G-only addressability-gap / M-only context) and `m_fragment` = the relevant-neighborhood slice G *touches* (seed by shared relation/individual, one hop along same-individual / line-of-identity, budget-capped, the rest reported as a **horizon**). Wired into `/agon/interpret` as a `render_m` block and drawn in `agon.html` (the legend + a small M-fragment board in the reading strip). The in-view-set design's recommendations (c)+(d); see `docs/THE_MINIMAL_IN_VIEW_SET.md`.
  - `model_revision.py` — the step by which a model **M transforms through ongoing dialog**, enacting the **EPG episode-outcome taxonomy** (docs/EXEMPLARS.md §6, docs/ENDOPOREUTIC_GAME_GUIDE.md §"Taxonomy"). `REVISION_TAXONOMY` is the M-revising subset of the disposition taxonomy, each entry carrying its **Peircean mode** (induction/deduction/abduction/convention) + structural **kind** (enlargement/relinquishment): `new_fact` (3a, induction), `generalization` (Case 8, induction · a law), `conditional_acceptance` (3e, deduction), `abductive_hypothesis` (3b), `definition` (3d), `reductio` (2d), `theorem_registration` (1a), `challenge_to_M` (2b, abduction). Primitives: `assert_fact`/`add_rule` (enlargement = juxtapose a posit/law onto M's sheet) + `retract_relation` (drop a sheet fact) + `retract_subgraph` (relinquish a sheet-level *law/cut* by a genuine Dau **ERA**, verified by reconstruction) — dispatched by `revise_with_disposition`. `revision_taxonomy(key)` rejects a non-revising disposition (`redundancy`/`rejection`/…). A revised M is a diachronic UoD carrying its own history. Geometry-free. Two exemplars: `dialogue_model_revision` (the insurance audit — `new_fact` only, FALSE→TRUE→FALSE→TRUE) and `dialogue_swan_revision` (walks the taxonomy across all three modes — induction observes/generalizes, the law absorbs a newcomer, abduction meets the black swan and relinquishes the over-general law; FALSE→TRUE→TRUE→TRUE→FALSE). **Surfaced** in Organon by the **audit lens** (`web_viewer/js/audit-lens.js` over `GET /organon/uods/{id}/audit`) — a standing proposal G (the UoD's declared `audit-proposal` annotation, or any EGIF the reader types) peeled against every successive M, drawn as the verdict ribbon with each transition labelled by its disposition · mode.
  - `agon_evolution.py` — **automated model development: the Agon as the engine of change** (docs/AUTOMATED_MODEL_DEVELOPMENT.md). *Not* a deterministic cellular automaton — a **generation is a round of the game**: ① a `Proposer` (the *membrane*) scribes a candidate G → ② `peel` tests it against the developing M (`semantic_game.evaluate` over a materialized `CorpusOracle`) → ③ an `Agonothetes` **panel** of policy-agents (`ObserverAgent` new_fact · `GeneralizerAgent` generalization · `ChallengerAgent` challenge_to_M, resolved by priority — the negotiation where emergence lives) negotiates a disposition → ④ `revise_with_disposition` injects it → ⑤ a `UsageLedger` decays relations that fell from use (disuse-decay = the only bound on the *unbounded* sheet, vs Conway's bounded plane). `run(...) → EvolutionResult` (a `TransformationChain` + `DOMAIN_MODEL` UoD + per-round `RoundOutcome`s + a discovery digest); deterministic given a deterministic proposer. **Stage 0 = a closed membrane** (`CorpusProposer` replays a pool; `MutationProposer` recombines M's unary relations into candidate subsumption laws — discovers the lattice the corpus already commits to); the open membrane (LLM/human/online) plugs into the same `Proposer` socket later. Fed the swan pool it **reproduces the hand-played `dialogue_swan_revision` trajectory on its own** (new_fact→generalization→new_fact→challenge_to_M). Additive, geometry-free, not protected. Demo: `tools/build_agon_evolution_demo.py` → the `agon_evolution_swan` corpus UoD, viewable through the **audit lens** with no lens change.
  - `agon_llm.py` — **the *automated Endoporeutic Game*, all three LLM roles (Stages 1–3)** — Graphist·doubt / Grapheus·defense / Agonothetes·judge — playing the game autonomously under the incorruptible mechanical peel (docs/AUTOMATED_ENDOPOREUTIC_GAME.md). Governing principle *the LLM argues, the calculus decides*; **every LLM move is reduced to a calculus artifact and re-checked** before it counts. All three share the `nl_to_logic` contract (optional `nl` extra `ANTHROPIC_AVAILABLE`, **injectable client**, **forced tool-use** via `_call_tool`, **never raises**). (1) **`LLMGraphist`** (Stage 1, an `agon_evolution.Proposer`) — reads M's **thin spots** (`attention_brief` — relations with ≤1 instance, ungrounded laws, lonely individuals) and voices **one doubt** via `propose_graph` (FOL + `doubt_type`); Arisbe reduces it (`nl_to_logic.build_proposal` → EGIF, `_normalize_fol` maps Capitalized FOL predicates onto M's lowercase vocabulary), so an unparseable doubt never reaches the loop; bootstraps a model **from the blank sheet**. (2) **`LLMGrapheus`** (Stage 2, an `agon_evolution.PolicyAgent`) — given M, the proposal, and the verdict (+ witness/counterexample), votes the **minimal** model-revising disposition from `REVISION_TAXONOMY` via `defend_model`; **reduce-to-artifact + re-peel**: the EGIF payload is normalized (`_normalize_egif`), *applied* (`revise_with_disposition`), and the proposal *re-peeled* — a defense that won't apply retries then abstains. Drop into a panel as `Agonothetes([LLMGrapheus(...)])`. (3) **`LLMAgonothetes(Agonothetes)`** (Stage 3) — overrides **resolution**: an LLM judges among the votes cast via `judge` (returns an **index** — cannot fabricate a disposition or overrule the verdict), falls back to mechanical highest-priority on any failure, never fires the LLM with nothing to judge (single/unanimous vote). **Branch-the-DAG:** on irreducible disagreement it names dissenting votes to carry forward as siblings; `agon_evolution.run` reads the optional `panel.branch_votes` hook and **forks the diachronic DAG from the pre-round state** for each (two chain steps share a `from_state_id`), resuming the main line — the mechanical panel has no such hook, so the closed loop stays linear/backward-compatible. Reproduces the swan trajectory *even when the LLM emits Capitalized predicates*. **Hardening (2026-07-02):** every source-derived string (M's sheet/vocabulary, proposals, witnesses, other agents' rationales, retry feedback) enters a prompt only inside a breakout-neutralized `<data>…</data>` fence (`_quarantine`) with the standing `_DATA_GUARD` in each system prompt — the prompt twin of the EGIF sanitizers, shrinking the disposition-bias channel a crafted wiki edit would use; and `RoleTelemetry` on all three roles splits **error** (client/SDK failed) from **judgment** (reachable, abstained on content) from **fallback** (judge → mechanical), so a dead API key can't silently degrade the LLM loop to mechanical while looking healthy. Demos `tools/build_llm_graphist_demo.py` (Stage 1) + `tools/build_llm_epg_demo.py` (all three, key-gated).
  - `agon_metalearning.py` — **the game studying the game** (docs/AUTOMATED_ENDOPOREUTIC_GAME.md §6): the microscope on the EPG's own rules, reading only the `EvolutionResult` a `run` returns (deterministic, geometry-free, no §3.3 obligation — works on the *mechanical* loop, no LLM). `situation_of` classifies each round by `verdict:shape` (ground / law / counterexample / negation); `episodes_from` yields the `(M, G, verdict, slate, disposition, did-it-stick)` mining tuples (stickiness = did the move survive to the final M — a `generalization` later relinquished reads `stuck=False`). `resolution_principles` mines each situation's dominant disposition + **stability** (1.0 = a discovered resolution principle; a split = **thrash** = ambiguity/missing rule) + stick-rate; `friction_map` ranks situations by disagreement (distinct dispositions voted + branches — lights up under the multi-agent LLM panel); `gaps` flags inconsistently-handled situations; `stability_report` + `run_ablation` (fresh proposer per variant) measure stabilization across parameter arms. **Learning from disputes:** `is_stuck` (public stickiness reader) + `DisputeEpisode` + `mechanism_principles` (which resolution mechanism produces *durable* knowledge — stick-rate by mechanism) + `edit_war_friction` (the contested frontier, ranked) + `unresolved_frontier` (the ◇-contested horizon) turn a wiki-dispute run into learned findings. **Stickiness is decay-aware (2026-07-01):** a game-performed erasure reads `stuck=False` (durability evidence) but disuse-decay reads `stuck=None`+`erased_by_decay=True` — excluded from stick-rates, counted in `MechanismPrinciple.decay_erased` (without the split a decay-bounded live run's stick-rates are working-set noise: 125/150 episodes decayed in the first watched Wikidata run); `mark_decayed` + `mark_relinquished` retro-mark the cross-segment aggregate the runner accumulates. **Poise (the §4d observable, BUILT):** `poise_report` (tumbling windows read for engagement/settlement/absorption; failures named by pole — rigidity vs thrash; stumbles located, recoveries measured) + `poise_from_digests` (per-segment live reading) — perspectival, comparative, never a target. Demo `tools/build_metalearning_demo.py`; tests `test_agon_metalearning.py`.
  - `discourse_membrane.py` — **the first *open* membrane** (§4b), raise-only flavour, offline+replayable (CI-safe; a live source attaches at the same `Proposer` socket): `DiscourseFeed` replays **dated, sourced** propositions (`DiscourseItem(day, source, egif, deny=)`) one per round — a day is a generation — driving `run` like the closed proposers, but from *outside* the corpus. The raise-only referee can't check the world, so `consistency_report` enforces only **cross-source consistency** — it surfaces `P@A` vs `¬P@B` as *contested* (for a `challenge_to_M` or the Stage-3 Agonothetes' DAG branch to dispose of), modelling *the discourse, not the world*. `contested_contents` names the ◇-disputed points the modal lens reads as unsettled. Tests `test_discourse_membrane.py`.
  - `resolving_membrane.py` — **the first *raise-and-resolve* membrane** (§4b), the flavour with **world-teeth**; offline+replayable (recorded outcomes; a live API attaches at the same `Proposer` socket). **M's prediction is the peel:** `peel(M, claim, closed=False)` is M's open-world forecast (materializes M's laws → M bets on individuals it was never told about; UNKNOWN = an abstention, not a false guess), taken *before* the outcome is folded in. `ResolvingItem(claim, happened, world_egif)` carries the world's verdict; `ResolvingFeed` records the forecast in a `PredictionLedger`, then hands the ground truth to the loop where the **existing mechanical panel** disposes it (confirmed fact → `new_fact`; a swan-that-is-not-white refuting M's standing law → `challenge_to_M`, the world relinquishing the over-general law — `run` gained `seed_laws` so a law M *carries* is visible to the Challenger). `PredictionLedger` tallies hits/misses/abstentions + `net_score`/`accuracy`; `select_best` ranks competing theories by track record — *the world selecting against the over-general theory* (the Robot-Scientist teeth). No new referee (the outcome is *data*; the calculus still decides); correspondence-not-truth holds (a resolved market is low-warrant; M self-certifies a track record, not truth). Demo `tools/build_resolving_membrane_demo.py`; tests `test_resolving_membrane.py`. **Next:** a live source behind `ResolvingFeed` (prediction-market / sports / weather API).
  - `wiki_dispute_membrane.py` — the recommended *first real* open membrane (§4b), the source with **conflict + resolution structure** (between raise-only and raise-and-resolve): a `WikiDispute(claim, edits, resolution)` is an **edit war** (`WikiEdit` asserts/reverts — `reverts` = the contestedness signal) ending in a **resolution** with an editorial *mechanism* (`reliable_source` / `admin` / `consensus` / `unresolved`), not a physical verdict — so warrant differs by mechanism and a reliable source can **overturn** a prior consensus. `WikiDisputeFeed` (a `Proposer`) replays a recorded dispute record one dispute/round, scribing each resolution's ground truth for the mechanical panel (consensus generalization admitted; reliable-source counterexample → `challenge_to_M` relinquishes the over-general law via `seed_laws`/the Challenger; unresolved → entertained low-warrant). **The payoff:** `WikiDisputeFeed.episodes(result)` feeds the run into `agon_metalearning` (`mechanism_principles`/`edit_war_friction`/`unresolved_frontier`) — *learning which resolution produces durable knowledge*. Demo `tools/build_wiki_dispute_demo.py`; tests `test_wiki_dispute_membrane.py`.
  - `live_runner.py` — **running the automated game against a *live* source, bounded + paced + checkpointed** (docs/AUTOMATED_ENDOPOREUTIC_GAME.md §10). The round loop is **super-linear in |M|** (measured ~4ms/round at |M|≈25 → ~1.1s at |M|≈250; the peel forward-chains M each round + `ProofChain` snapshots the whole graph and holds every state in RAM), so an unbounded live run degrades on rate/memory/disk together. Two controls: (1) **disuse-decay bounds |M|** (`LiveRunConfig.ttl`) applied by the runner **across global rounds** (not per-segment, which would reset every segment) → per-round cost/memory/disk stay flat; (2) **segment + checkpoint + prune** — one segment per poll (batch, `segment_cap`), each saved via `TomosService.save_uod_with_chain` (§3.3 at the write) then the in-RAM `ProofChain` is **dropped**, carrying only M (EGIF) + live laws forward. `LiveSource` Protocol (`fetch`/`exhausted`) + `ReplaySource` (offline, CI; a real wiki/market API replaces it alone) + a `feed_factory` wrapping a batch into any membrane. Pacing (`min_interval_s`), stops (`max_rounds`/`max_seconds`/`stop_file`/`max_m_relations`), injectable `clock`/`sleep` (deterministic tests). Evaluation = a per-segment `SegmentDigest` (rounds/|M|/dispositions/decayed/branched/elapsed) + optional membrane-specific `evaluate(feed, result)` (e.g. ResolvingFeed accuracy, WikiDisputeFeed `mechanism_principles`). A batch larger than `segment_cap` is **queued, never truncated** (the cap is checkpoint cadence, not coverage), and the runner accumulates the feed's episodes across segments as `LiveResult.episodes` with decay + cross-segment relinquishments retro-marked (`mark_decayed`/`mark_relinquished`) — the honest long-run §6 input. **Crash/resume (2026-07-02):** `LiveRunConfig.state_path` persists the carried state per segment (post-decay M, laws, global counters, the disuse ledger — atomic write); `LiveRunner.resume` continues where a killed process stopped, decay clock continuing not resetting — a crash loses at most the in-flight segment. Demo `tools/build_live_runner_demo.py`; tests `test_live_runner.py`.
  - `wikidata_source.py` — **the first *live* source** (§4b/§10): Wikidata (structured → no NL parsing; public, no auth). A statement (item+property+value) → a ground binary fact `(prop "item" "value")`; a **reference** → provenance (`reliable_source` vs bare `consensus`); a **rank** → resolution (`preferred`/`normal` stand; `deprecated` = relinquishment); **competing values** → contestation. `WikidataStatement` + `statements_to_disputes` → `WikiDispute`s; `WikidataSource` (a `LiveSource` over an injectable fetch — CI offline on fixtures; `wbgetentities_fetch` is the real stdlib-`urllib` call, never hit in CI). Drives the whole pipeline (`LiveRunner`+`WikiDisputeFeed`+§6) unchanged. Two additive pieces make deprecation/overturn dispose **without an LLM**: `model_revision.retract_atom` (drop one sheet atom by relation+labels) + `agon_evolution.ContradictionAgent` (opt-in panel agent: a sourced denial `~[ (rel …) ]` of a standing atom → `retract_fact` that atom). Verified: a reliable source overturns a bare value mechanically. **Legibility (2026-07-01):** P/Q ids resolve to labels by default — `collect_ids`+`resolve_labels` (pure, offline-tested; an unlabelled id stays an id, never fabricated) + `wblabels_fetch` (batched 50/request; asks `en|mul` — since 2024 a language-independent name like Q42's "Douglas Adams" lives in the **`mul`** label, not `en`); the shared `_api_json` verifies TLS via `certifi` (stock stdlib contexts lack a CA bundle on MacPorts/pyenv) + sends a Wikimedia-etiquette User-Agent. First **watched live session** run 2026-07-01 (150 statements, 3 entities): |M| held at ≈ttl, batches queued not truncated, cross-segment overturn read correctly. **Legibility tripwire (2026-07-02):** `unresolved_fraction` + per-poll `WikidataSource.legibility` — a spike in bare-P/Q-id fraction surfaces silent label degradation (the `mul` failure mode) in the monitoring stream instead of M quietly filling with Q-ids. **Run-1 kit (2026-07-02):** `RotatingWikidataSource` — the rotating frontier (lazy per-poll fetch so the runner's pacing paces the API; `crawl` grows the queue from entity-valued values; `frontier_cap` + `per_entity_cap` bound growth and hub degree with drops *counted*; one `LabelCache` per run — unseen-only, negative-cached; `save_state`/`load_state` so `--resume` continues the crawl) + `record_poll`/`replay_polls` (every poll → JSONL → the run replays offline; the determinism canary) + driver `tools/run_live_wikidata.py` (side-store checkpoints under `runs/`, STOP-file, `--resume`, per-segment digests, §6+poise summary; findings → `runs/RUN_1_LOG.md` against §11's pre-registered priors). **Run-2 kit (2026-07-02, §12):** `rc_ids` (pure payload→ids) + `recentchanges_fetch` (rcend-continuation, bots excluded by default) + `RecentChangesSource` — the **change stream**: never-exhausting `LiveSource` (runner stops are the only ends), samples live contestation and *revisits* edited entities so a deprecation meets its still-standing target (the RUN_1_LOG F2/F3 findings made operational; headline test: the cross-poll overturn fires and `mechanism_principles` actually differentiates). Driver `--source recentchanges`. Live smoke: reliable_source-heavy (49:13 vs the crawl's consensus-heavy) — the stream and the settled surface are evidentially different samples. Demo `tools/build_wikidata_demo.py` (`--live Q42 --limit 60 --ttl 30` hits the real API); tests `test_wikidata_source.py`.
- `modal_query.py` — read **◇ / □ off the diachronic branching DAG** with no modal mark (the *trajectory reading* of `docs/MODALITY_WITHOUT_GAMMA.md` §1): `possibly`/`necessarily` over a chain's reachability (`reachable_states`/`leaf_states`) — ◇φ = "some legal trajectory scribes φ", □φ = "every legal trajectory scribes φ" (*possibility is branching, necessity is convergence*). Predicate helpers `scribes_relation`/`equals_graph`/`is_blank`; `over="states"|"leaves"`. Geometry-free (DAG + EGI content only), adds no §3.3 obligation. The `possible_and_necessary` exemplar demonstrates it. **Surfaced** in Organon by the **modal lens** (`web_viewer/js/modal-lens.js` over `GET /organon/uods/{id}/modal`) — relations scribed across the reachable sheets split into □ Necessary / ◇ Possible columns, with the worlds the modality ranges over and a `states`/`leaves` toggle.
- `reference_resolution_check.py` — the **reference / transclusion node** validation harness (ROADMAP #3, additive, no protected-core change): proves the law a reference node must satisfy, **`RESOLVE ≡ INLINED-AND-ATTESTED`** (the analogue of `attest_overview`). Per candidate: R1 resolve-equals-inline (`same_graph`), R2 resolved-is-§3.3-attested (`layout_fn` injected so the module stays geometry-free), R3 recoverable (`fold`, where an inverse exists), R4 honest horizon (unresolved named). De-risks the core change before opening `egi_core_dau`. See `docs/REFERENCE_AND_TRANSCLUSION_NODE.md`.
- `reference_node.py` — the reference node, **increment 1 (intra-UoD, additive)**: a **Form-2 reference edge** (a defined-relation spot) + an OVERLAY `ReferenceMark` (serialisable, keyed by edge id, carrying target/kind/origin/`warrant="low"`, kept *beside* the EGI — `egi_core_dau` untouched). `ReferenceResolver` Protocol (shaped like `cl_import_resolver.ImportResolver`) with `DefinitionReferenceResolver` (resolution = `definitions.expand_at`, inverse = `fold`) + `ChainReferenceResolver` (schema / corpus-UoD resolvers drop in additively for increment 2). `mark_reference` / `resolve_reference` / `refold_reference` / `attest_reference` (the boundary hook → `reference_resolution_check`); `reference_horizon` / `render_marks` feed the **render glyph** (`simple_svg_renderer`'s `reference_marks=` draws a dashed spot + "+N beyond view" badge — pure chrome, no DTO/§3.3 change, off by default). Resolving a reference *is* co-assertion (conjunction; the double cut is inert) — a soundness guarantee intra-UoD; cross-UoD is the deferred use(scroll-import)/mention(2nd-order-naming) fork.
- `tomos_service.py` — Unified corpus API. `save_uod` / `load_uod` attest §3.3 at the save/load boundary. `save_uod_with_chain(uod, chain)` + `load_chain(uod_id)` persist a workshop chain (V1 linear) as `history/chain.jsonl` + `history/states/<id>.egi.json` alongside the UoD record; §3.3 fires inside `save_uod` before any chain files are written, so a refusal aborts cleanly with no half-saved chain on disk. `TransformationChain` / `ChainStep` are the slim on-disk shape (NOT a hydration of the protected `EGITransformationHistory`).
- `z3_semantic_validator.py` — Z3 SMT-solver semantic validation
- `graph_isomorphism_engine.py` — NetworkX VF2 matching for goal detection
- `web_api/` (FastAPI) + `web_viewer/` (static HTML/JS) — the canonical user interface. All three mode routes are live: `/organon` (read-only archive, both load+render boundaries §3.3-attested), `/ergasterion` (workshop / composition — regime-1 drafts; a session holds a forest of branches with move-by-move navigation; output goes to a regime-1 **scratch** store or is **sent to Agon**, never straight to the corpus — there is no direct workshop→corpus route. Composition is **freeform draw-then-read** (`web_viewer/js/freeform-canvas.js`): typed marks placed/dragged/erased on a free canvas with no live EGI, read into a sign only at gate ① via `read-drawing` (preview) / `fix-drawing` (commit) — backed by `drawing_validity` + `drawing_to_egi`. A **Graph↔Argument** two-mode switch makes fixed/unfixed unmistakable and enforces "no rules on an unfixed graph; no meaning-change on a fixed one"; "Edit base graph" re-opens a fixed/corpus graph as an independent copy via `state-drawing` seeding; **challenge mode** — pick a target linear form, draw it freehand, get graded with `same_graph` + the legible diff), `/agon` (Endoporeutic Game arena — the **contest** register is the hot-seat transformation game; the **interpretation register** is the episode *given M, then G*: choose a reference model M (the picker lists curated scenarios + corpus UoDs, optionally **materialized**), `/agon/interpret` peels G against it → verdict + transcript + witness/counterexample, `/agon/where-it-holds` runs the inverse pivot, and the Agonothetes' disposition taxonomy is annotated by the verdict). The mode contract: a graph reaches the attested corpus only by being tested through Agon or as a style-only reprojection of an attested graph (§3.3 attests *correspondence, not truth*). A shared left-column nav (`web_viewer/js/mode-nav.js`) links the three modes + home. Rendering is one engine end to end: server-side EGI→SVG (`layout_service` + `simple_svg_renderer`, §3.3-attested) and client-side **`web_viewer/js/diagram-viewer.js`** (`DiagramViewer.render(svg, {camera:'fit'|'hold', dolly, transition})`) — the single pan/zoom/camera component all three modes use.

### Linear Format Support (all production, round-trip tested)

| Format | Module | Corpus tested |
|--------|--------|--------------|
| EGIF | `egif_parser_dau.py` / `egif_generator_dau.py` | 57+ examples |
| CGIF (ISO/IEC) | `cgif_parser_dau.py` / `cgif_generator_dau.py` | 40+ examples |
| CLIF (Common Logic) | `clif_parser_dau.py` / `clif_generator_dau.py` | 35+ examples |
| FOPL | `chapter18_fopl_translation.py` | Φ/Ψ bidirectional |
| JSON | `egi_io.py` | With layout deltas |

### Data Model Invariants

- **EGI is immutable**: use `.with_vertex()`, `.with_edge()` — never `.add_*()`
- **Import pattern**: `from module_name import Foo` (not `from src.module_name`)
- **UoD state**: `State_n = (EGI_n, LayoutDeltas_n)` — deltas persist through transformations

### Beta Graph Support (FOL)

- **Beta graphs** use shared vertices across cut boundaries (lines of identity)
- EGIF: `*x` = defining label (new vertex), `x` = bound label (existing vertex in enclosing scope)
- `~[ (P *x) ~[ (Q x) ] ]` = ∀x(P(x) → Q(x)) — one shared vertex
- `SubgraphClosureValidator` with `context_area` treats ancestor-area vertices as free
- `IterationRule` extends lines of identity (reuses source-area vertices, no copy)
- `RuleInteraction` protocol: `begin_interaction` → `advance_interaction` → `apply_interaction`

## API Discovery

**Never guess function signatures.** The complete API is documented in
`docs/ARISBE_CORE_API_REFERENCE.md` (regenerated from
`tools/core_protection_system.py`'s protected-modules set):

```bash
grep -i "function_name" docs/ARISBE_CORE_API_REFERENCE.md
cat docs/CORE_API_USAGE_GUIDE.md          # Common development patterns
uv run python tools/extract_core_api.py   # Regenerate the reference
```

Before starting any implementation task, check whether a solution already exists:
```bash
python tools/context_awareness_system.py --check "task description"
```

## Key Documentation

- `AGENTS.md` — Developer guidelines with code patterns and usage examples
- `docs/GETTING_STARTED.md` — **The role-aware on-ramp for new users** (ROADMAP #15): assumes no logic background, then branches by expertise (newcomer / ontologist / logician / mathematician / Peirce scholar). A map of doors, not a manual.
- `docs/EXTERNAL_SOURCES_AND_IMPORT.md` — **The consolidating import doc** (ROADMAP #16): how outside information gets in — the low-warrant floor, the two families (formal-file translators → `kind=ontology` UoDs; human-read material via `/import`), the tool/module map, the forward edges.
- `docs/AUTOMATED_MODEL_DEVELOPMENT.md` — **design-of-record for `agon_evolution.py`**: automated model development with the Agon (not deterministic rules) as the engine of change — a generation is a game round (produce→test→negotiate→inject→decay); the membrane (closed → open) staging; why an *unbounded* sheet is bounded only by selection-from-outside.
- `docs/AUTOMATED_ENDOPOREUTIC_GAME.md` — **design-of-record for `agon_llm.py`**: the automated Endoporeutic Game (three LLM roles — Graphist·doubt / Grapheus·defense / Agonothetes·judge — under an incorruptible mechanical referee); the doubt-engine portfolio + recurrent real-world membranes (raise-only vs raise-and-resolve); branch-on-disagreement; the meta-learning payoff (the game studying its own resolution principles); prior art; the Stage-1→2→3 staging.
- `docs/EXEMPLARS.md` — **Catalogue of the worked proof exemplars + domain-model boards** seeded into the corpus (so Organon has proofs to read and Agon has boards to play): the propositional proof quartet (`tools/build_propositional_exemplars.py`, exercises all six rules), the `zoo_world`/`harbor_town` domain models (`tools/build_domain_model_exemplars.py`), and the curated Agon episodes wiring them into the picker (`agon_models.py`). Tests: `test_new_exemplars.py`.
- `docs/LINEAR_GRAPHICAL_CORRESPONDENCE.md` — **The central contract.** Read before touching anything that produces or consumes (EGI, LayoutDTO) pairs (transformation rules, layout, rendering, sessions, the three modes, the Endoporeutic Game).
- `docs/CHAIN_OF_SEMIOSIS.md` — **The Peircean grounding.** Why a reasoning episode is a chain of sound, attested sign-transitions; why every rule application is an attestation event; how Arisbe's provenance/immutability model relates to Git/Datomic/wikis and where it departs (the sound-step requirement). Read for the *purpose* behind the chain model, the regimes, and the Ergasterion promotion boundary.
- `docs/UNIVERSE_OF_DISCOURSE_ARCHITECTURE.md` — Core paradigm (read before touching UoD/history)
- `docs/DAG_HISTORY_ARCHITECTURE.md` — Branching transformation history
- `docs/ARISBE_CORE_API_REFERENCE.md` — Auto-generated API reference
- `docs/RETURN_TO_DEVELOPMENT.md` — 5-minute context recovery for the returning author
- `tomos/` — 87+ canonical EG examples with EGIF/CGIF/CLIF/FOPL variants

## Mathematical Foundation

Code chapters correspond to Dau's formal textbook:
- Ch. 14/15 → `formal_transformation_rules.py` (six transformation rules, Beta-aware)
- Ch. 14/15 → `rule_interaction.py` (headless stepwise protocol for all rules)
- Ch. 16–17 → `ligature_manipulation_rules.py`, `chapter17_soundness_evaluation.py`
- Ch. 18 → `chapter18_fopl_translation.py` (linear format Φ/Ψ translations)
- Ch. 20 → `syntactic_equivalence_checker.py`, `chapter20_syntactic_equivalence_fixes.py`

## Testing (~1000 passing, 35 skipped)

Key test files:
- `test_correspondence_invariant.py` — All six §7 test shapes from `docs/LINEAR_GRAPHICAL_CORRESPONDENCE.md` (totality/injectivity, containment, incidence + arg-order, identity 3-way, transformation invariance, regime-3 non-interference) against the tomos corpus
- `test_correspondence_attestation.py` — Module contract: corpus-wide happy path plus adversarial unit tests confirming each §3.3 property's failure raises `CorrespondenceViolation`
- `test_presentation_ops.py` — Regime-3 API contract: happy + refusal paths for `move_vertex` / `move_predicate` / `reshape_cut` / `move_cut` / `reroute_ligature`
- `test_layout_service_attestation.py` — The web boundary hook fires on a real UoD and refuses when the engine returns a corrupted DTO
- `test_chain_persistence.py` — Transformation-chain JSONL round-trip; §3.3 refusal at save_uod_with_chain leaves no chain files on disk
- `test_ergasterion_routes.py` — Workshop route contract: session lifecycle, RuleInteraction-driven apply, regime-1 drafts don't fire corpus-record §3.3, move-by-move navigation (a UoD's worked sequence hydrates + any state renders), branch-on-edit (apply from an earlier state forks; branches switchable), scratch store (save/list/open/delete; never the corpus). (Direct workshop→corpus promotion was retired; its §3.3 mechanism lives in `test_chain_persistence.py`.)
- `test_reference_resolution_check.py` — the reference-node law harness: `RESOLVE ≡ INLINED-AND-ATTESTED` holds on real definition + transclusion candidates (R1–R4, real-ELK R2); falsifiers bite (doctored inline, lossy refold, rejecting attest, resolve-raises) so the pass is earned
- `test_reference_node.py` — reference-node increment 1a: the overlay mark round-trips, authoring refuses an unresolvable edge (R4), resolution equals the independent raw fixture (R1), refold recovers the host (R3), the boundary hook attests / bites, the chain resolver dispatches (seam extension-readiness)
- `test_reference_glyph.py` — reference-node increment 1b (the render glyph): `reference_horizon` counts the hidden body, `simple_svg_renderer`'s `reference_marks=` draws a dashed spot + "+N beyond view" badge *only* when marked (off by default), and the glyph changes no attested DTO geometry
- `test_eg_reader.py` — drawn→EG round trips (`read(render(egi)) == egi`) corpus-wide, both engines/styles + full argument order; `TestFreeformRobustness` pins the reader on *human* geometry (the freeform de-risk)
- `test_peirce_latex.py` — authentic-Peirce LaTeX export: corpus-wide totality+traceability over all 29 UoDs (every cut/predicate/vertex id in the `.tex`, macro counts match the EGI); reader-faithfulness (`reading_matches_egi(read_drawing(dto), egi)`) on a representative subset + a falsifier (doctored DTO reads back different); **actual `pdflatex` compilation** of a curated handful (alpha/beta-teridentity/scroll/isolated-vertex/empty-cut/TeX-special-name, gated on a LaTeX toolchain, `article` fallback when `standalone.cls` absent); the PEP delta path (`export_egi(deltas=…)` publishes the adjusted arrangement); format-registry + dispatch wiring; **phase 2** — the opt-in iconic scroll glyph is ink-only (faithfulness preserved either way) and the worked-chain document assembles a captioned figure per step + compiles. Route coverage (`test_export_routes.py`): `/export` deltas pass-through + malformed-delta rejection, `/export/chain` worked-chain + CHAIN_NOT_FOUND
- `test_layout_learning.py` — the drawing→EGI learning loop: recovers a known vertex/predicate move + cut reshape (inverse of `apply_deltas`); identical layouts yield no deltas; round-trip (learned deltas replay to reproduce the drawn arrangement, §3.3-attested); the loop generalises a learned placement onto an untouched sibling
- `test_drawing_validity.py` — fix-time validity pass: each error/warning + clean engine layouts raise zero errors
- `test_drawing_to_egi.py` — drawing→EGI builder: corpus round-trip via `same_graph` (both styles, nested cuts, argument order, constant-vs-generic)
- `test_ergasterion_freeform.py` — freeform routes: `read-drawing`/`fix-drawing` round-trip to `same_graph`, ill-formed refusal, JS-serialize↔backend contract, `state-drawing` seeding, editable corpus copies
- `test_ergasterion_freeform_e2e.py` — headless-Chromium E2E (Playwright): draw→read→fix, the Graph↔Argument round-trip, corpus edit-base consistency, spot snapping (skipped if Playwright/Chromium absent)
- `test_egi_diff.py` — legible diff: same-graph→no findings; missing/extra/scope/incidence/order findings
- `test_challenge_mode.py` / `test_ergasterion_challenge.py` — challenge mode (freeform step 4): the difficulty gradient parses, grading passes a same-graph attempt regardless of surface form and fails wrong ones with the right finding; routes grade a freehand drawing (ill-formed ink → validity feedback, non-mutating)
- `test_domain_oracle.py` — the `DomainOracle` contract: `resolve` CONFIRMED/UNKNOWN/DENIED (open vs closed), constants by label, arg-order via `nu`, provenance, cut refusal; `match_atoms` enumerates all bindings
- `test_semantic_game.py` — the peel: ground atoms, negation, the scroll/universal open-vs-closed, existential witness selection; structured `winning_witness` / `counterexample` (the student's Whale, the physician's Biscuit)
- `test_model_materialization.py` — Horn forward-chaining (syllogism, chained rules, recursive transitive closure, binary join) to the least Herbrand model; non-Horn shapes skipped with the right reason
- `test_theory_query.py` — theory query / freeze-a-witness (`entails`): applicability (only a range-restricted Horn universal), direct + transitive subsumption TRUE, underivable FALSE (wholly Horn) vs UNKNOWN (non-Horn residue), multi-var typing rule, conjunctive head; the real corpus ontologies (SUMO subsumption theorems, Porphyry ladder + disjointness residue, FOAF typing chains through subsumption)
- `test_owl_import.py` — the OWL→CLIF→EGI pipeline (`tools/owl_to_clif` + `domain_model_importer.from_owl_*`): each OWL 2 functional-syntax axiom form → its Common-Logic shape (subsumption / equivalent / disjoint / sub-property / domain-range / inverse / symmetric / transitive / intersection body / someValuesFrom head / assertions / sameAs); untranslatable constructs (cardinality, union, annotation, ⊑Thing) reported not dropped; IRI/prefix → local name; the `zoo.ofn` fixture imports to an EGI; the loop closes — an OWL-imported ontology decides subsumption / intersection / transitivity theorems via `theory_query.entails`
- `test_agon_interpretation.py` — the interpretation register routes: the five persona episodes reproduce their verdicts; `set-model`; verdict-annotated dispositions (full taxonomy, nothing auto-asserts); materialize makes the syllogism hold; the inverse pivot ranks domains (holds/partial/independent/contradicts) with the residue; **render-M** (the `render_m` block: vocabulary legend + the neighborhood fragment G touches, with horizon)
- `test_m_render.py` — render-M unit contract: `vocabulary_overlap` splits shared / G-only (addressability gap) / M-only; `m_fragment` keeps what G touches + one hop (same-individual or line of identity), reports the horizon, respects the budget, flags nested structure, and is empty when M's vocabulary is alien
- `test_organon_routes.py` — Archive route contract: corpus listing, UoD detail, both load+render attestation hooks fire per detail request; the two diachronic *reading* routes — **modal** (`/modal`: ◇/□ off the branching `possible_and_necessary` history — □cold, ◇cloudy/◇calm; `over=leaves`; clean on a synchronic UoD) and **audit** (`/audit`: the declared-proposal default flips FALSE→TRUE→FALSE→TRUE on `dialogue_model_revision`, explicit-proposal override, `NO_PROPOSAL`/`AUDIT_ERROR` clean failures). `test_organon_lenses_e2e.py` adds the **modal lens** (□/◇ columns + worlds + leaves toggle) and **audit lens** (verdict ribbon + pre-filled proposal + flip flags) browser checks
- `test_agon_llm.py` — the automated EPG's three LLM roles (Stages 1–3), CI-safe with a role-agnostic **scripted fake client** (`ToolClient` echoes the forced tool; no SDK/key). **Stage 1 (Graphist):** `_normalize_fol` maps Capitalized FOL onto M's vocabulary; `attention_brief` invites bootstrapping on the blank sheet and names thin spots; `propose` reduces FOL→EGIF, **retries on unparseable then recovers**, returns `None` (never raises) on failure/unreachable, avoids repeats; the headline — fed the swan doubts (as *Capitalized* FOL) the Graphist drives the mechanical panel to **reproduce the swan trajectory**; bootstraps from the blank sheet; §3.3-persists. **Stage 2 (Grapheus):** `_normalize_egif`; `LLMGrapheus.vote` admits the minimal fact and **re-peels** (verdict flips to TRUE), retries on an inapplicable defense then recovers, abstains (never raises) when it never applies or the client is unreachable; the headline — the LLM Grapheus (not the mechanical panel) drives the swan trajectory new_fact→generalization→challenge_to_M with the standing law flipping TRUE→TRUE→FALSE. **Stage 3 (Agonothetes):** `LLMAgonothetes.resolve` judges among the votes cast, is mechanical (no LLM call) on a single/unanimous vote, falls back to highest-priority on a bad index; the headline — a two-vote disagreement **forks the DAG** (two chain steps share the pre-round `from_state_id`, `outcome.branched` records it) while the mechanical panel stays linear. **Hardening:** the quarantine fences (breakout neutralized; the guard in all three system prompts; a hostile relation name/proposal/rationale reaches each role only inside a fence) + `RoleTelemetry` (error vs judgment vs fallback). A live test is skipped unless the `nl` extra + key are present
- `test_agon_metalearning.py` — the meta-learning instruments (§6), deterministic + LLM-free: `proposal_shape`/`situation_of` classify the round; `episodes_from` over the mechanical swan run captures the trajectory + **stickiness** (the superseded law reads `stuck=False`); `resolution_principles` finds a stable mapping and a hand-built **thrash** (split → stability 0.5); `friction_map` ranks disagreement most-contested-first; `gaps` flags an inconsistently-handled situation; `stability_report` says the swan run settled@3; `run_ablation` compares decay-off vs -on with fresh proposers; the decay/relinquishment split (a decayed fact reads `stuck=None`+`erased_by_decay`, a retracted one `stuck=False`; `mark_decayed` spares relinquishment episodes, `mark_relinquished` is atom-precise); the poise instrument (the swan run reads poised with one absorbed stumble; rigidity + thrash poles; recovery measured in rounds; `poise_from_digests` over a live stream)
- `test_discourse_membrane.py` — the raise-only membrane (§4b): the feed emits content in order wrapping denials in a cut, days = generations, exhaustion returns `None`, it drives `run` into a diachronic UoD (stops when exhausted); `consistency_report` flags cross-source `P@A`-vs-`¬P@B` conflict (matched by `same_graph`), reports none when sources agree, and `contested_contents` names the disputed proposition
- `test_wikidata_source.py` — the first live source (§4b/§10), deterministic + offline (the real `wbgetentities_fetch` never called): statement→EGIF-safe fact; `statements_to_disputes` maps rank→settled + references→mechanism + competing values→reverts; the source polls-then-exhausts; `from_fetch` calls the injected fetch per poll; `retract_atom` drops only the matching atom; the `ContradictionAgent` votes `retract_fact` on a denied *standing* fact and abstains otherwise; the headline — end-to-end a Wikidata deprecation + reliably-sourced replacement **overturns a bare value** (`retract_fact` then `new_fact`; final M keeps only the referenced value), no LLM; the label-resolution pure half (`collect_ids` first-seen P/Q ids only; `resolve_labels` substitutes known + keeps unknown, provenance carried, legible EGIF out); the legibility tripwire (`unresolved_fraction` + per-poll `legibility`)
- `test_live_runner.py` — the live runner (§10), deterministic + offline (ReplaySource + injected clock/sleep): source yields-then-exhausts; one segment per batch carrying M forward; the headline — **disuse-decay bounds |M| across segments** (stabilises at ≈ttl) vs. unbounded growth without it; every stop condition (max_rounds/max_seconds via injected clock/stop_file/max_m_relations); pacing sleeps between polls; checkpoints persist each segment as real saved UoDs; the `evaluate` hook feeds membrane-specific metrics (a WikiDisputeFeed's durable mechanisms) into the digest; an oversized batch is segmented not dropped; the cross-segment aggregate (`LiveResult.episodes`) reads a runner-decayed fact as decay (stick-rates uncorrupted) and a later segment's overturn as `stuck=False` (the Wikidata deprecation scenario reads consensus not-durable / reliable_source durable); resume continues a stopped run (global numbering, all facts carried, the decay clock continuing not resetting)
- `test_wiki_dispute_membrane.py` — the wiki-dispute membrane + the dispute-learning layer (§4b/§6), deterministic + LLM-free: reverts/editors counting, `ground_truth` by resolution, the feed drives+exhausts the loop; the headline — a **reliable source overturns a prior consensus** (dispositions generalization→challenge_to_M→new_fact; the consensus generalization `stuck=False`, the reliable-source challenge `stuck=True`); `mechanism_principles` learns reliable_source is durable + consensus is not + unresolved never durable; `edit_war_friction` ranks the fiercest edit war first; `unresolved_frontier` names the contested claim
- `test_resolving_membrane.py` — the raise-and-resolve membrane (§4b), deterministic + LLM-free: `classify`/`PredictionLedger` scoring (hit/miss/abstain, net, accuracy), `ground_truth` defaults; the headline — an over-general `swan→white` theory forecasts a non-white swan white and is **empirically falsified** (disposed `challenge_to_M` by the mechanical panel, net −1) while the correct `bird→flies` theory hits then abstains (net +1); `seed_laws` is shown to be what makes the standing law visible to the Challenger; `select_best` picks the better predictor — the world selecting against over-reach
- `test_agon_evolution.py` — the automated Agon-development loop: each panel agent in isolation (Observer admits/abstains, Generalizer refuses a false/vacuous law, Challenger fires only on a counterexample to a *standing* law, panel resolves by priority); `UsageLedger` staleness + decay erases a relation that fell from use; the headline — fed the swan pool the loop **reproduces the hand-played `dialogue_swan_revision` trajectory** (dispositions + modes + the audited proposal flipping TRUE→…→FALSE); reproducibility; the final M reflects the revisions; discoveries flag the anomaly + superseded law; a redundant proposal is a non-revising round; `MutationProposer` admits only supported subsumptions; and the trajectory **persists + §3.3-attests** at the corpus boundary
- `test_primer_route.py` / `test_primer_e2e.py` — the newcomer "first graph" primer (`web_api/routes/primer.py` + `web_viewer/js/primer.js`): `/primer/examples` serves the curated first graphs drawn by the real engine (scroll + empty cut present, each parses+renders); E2E — the home "New here?" door + the per-mode mode-nav link open the overlay with ≥3 live-drawn SVGs; a dragon chip deep-links into Ergasterion challenge mode (`?challenge=🐉N`)
- `test_epg_exemplar_scripts.py` — 16 Endoporeutic Game scenarios (outcomes, strategies, engine integration)
- `test_beta_proof_exercises.py` — 20 Beta graph tests (FOL, shared vertices, EGIF round-trips)
- `test_logical_proof_exercises.py` — Propositional tautology derivations (modus ponens, etc.)
- `test_rule_interaction.py` — Headless RuleInteraction protocol integration tests
- `test_subgraph_closure_validation.py` — Closure validator including Beta-aware checks
- `test_graph_isomorphism_engine.py` — VF2 isomorphism for IT- validation
- `test_tomos_parsing.py` — EGIF/CGIF/CLIF round-trip across 87+ tomos examples
