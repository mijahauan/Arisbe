# Arisbe - Existential Graph Implementation
# Core dependencies for EGIF parsing, EGI structures, and diagram visualization

# Core dependencies
PyYAML>=6.0
frozendict>=2.3.0

# Visualization and GUI dependencies
PySide6>=6.5.0
Pillow>=10.0.0

# Layout engine dependencies
graphviz>=0.20.0

# Computational geometry dependencies
shapely>=2.0.0

# Development and testing dependencies (uncomment for development)
# pytest>=7.0.0
# pytest-cov>=4.0.0
# black>=23.0.0
# mypy>=1.0.0

# Note: Standard library modules used: abc, dataclasses, enum, math, os, sys, threading, typing
# Note: Graphviz system package required (brew install graphviz / apt-get install graphviz)
# Note: Obsolete dependencies removed: pygame, tkinter backends, cassowary constraint solver
