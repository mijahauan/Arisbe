# Arisbe - Existential Graph Implementation
# Core dependencies for EGIF parsing, EGI structures, and diagram visualization
#
# IMPORTANT: Use conda environment 'CGIF' which has the correct package versions
# To activate: conda activate CGIF
#
# Core dependencies
PyYAML>=6.0
frozendict>=2.3.0

# Visualization and GUI dependencies
# Choose one: PyQt6 (preferred for Chapter 16 GUI) or PySide6
# PyQt6>=6.5.0
PySide6>=6.5.0
Pillow>=10.0.0

# Layout engine dependencies
graphviz>=0.20.0

# Computational geometry dependencies
shapely>=2.0.0
pyvisgraph>=0.2.2
rectpack>=0.2.2
numpy<2.0.0

# Spatial indexing for hierarchical EGI operations
rtree>=1.0.0

# Text extraction for PDF references
pdfminer.six>=20231228

# Network and graph analysis for semantic code analysis
networkx>=3.0

# Development and testing dependencies (uncomment for development)
# pytest>=7.0.0
# pytest-cov>=4.0.0
# black>=23.0.0
# mypy>=1.0.0

# Note: Standard library modules used: abc, dataclasses, enum, math, os, sys, threading, typing
# Note: Graphviz system package required (brew install graphviz / apt-get install graphviz)
# Note: Obsolete dependencies removed: pygame, tkinter backends, cassowary constraint solver
pathfinding>=1.0.0
